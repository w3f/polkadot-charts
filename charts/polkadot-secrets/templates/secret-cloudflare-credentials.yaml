{{ if not .Values.local -}}
apiVersion: v1
kind: Secret
metadata:
  name: cloudflare-credentials
  namespace: {{ .Values.namespace }}
data:
  api-key: {{ .Values.cloudflareApiKey | b64enc }}
{{ end }}
