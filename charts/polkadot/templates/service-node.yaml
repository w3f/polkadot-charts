apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
  labels:
    node: {{ .Release.Name }}
spec:
  ports:
  - port: 30333
    name: p2p
  selector:
    node: {{ .Release.Name }}
